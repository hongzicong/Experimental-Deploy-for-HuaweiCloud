import { PrePaidServer } from './PrePaidServer';
export declare class CreateServersRequestBody {
    private 'dry_run'?;
    server: PrePaidServer;
    constructor(server?: any);
    withDryRun(dryRun: boolean): CreateServersRequestBody;
    set dryRun(dryRun: boolean | undefined);
    get dryRun(): boolean | undefined;
    withServer(server: PrePaidServer): CreateServersRequestBody;
}
