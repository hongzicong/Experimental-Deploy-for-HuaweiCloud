import { Link } from './Link';
export declare class SimpleFlavor {
    id: string;
    links: Array<Link>;
    constructor(id?: any, links?: any);
    withId(id: string): SimpleFlavor;
    withLinks(links: Array<Link>): SimpleFlavor;
}
