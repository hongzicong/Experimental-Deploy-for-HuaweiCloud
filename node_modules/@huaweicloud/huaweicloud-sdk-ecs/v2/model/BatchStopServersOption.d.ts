import { ServerId } from './ServerId';
export declare class BatchStopServersOption {
    servers: Array<ServerId>;
    type?: BatchStopServersOptionTypeEnum;
    constructor(servers?: any);
    withServers(servers: Array<ServerId>): BatchStopServersOption;
    withType(type: BatchStopServersOptionTypeEnum): BatchStopServersOption;
}
/**
    * @export
    * @enum {string}
    */
export declare enum BatchStopServersOptionTypeEnum {
    SOFT = "SOFT",
    HARD = "HARD"
}
