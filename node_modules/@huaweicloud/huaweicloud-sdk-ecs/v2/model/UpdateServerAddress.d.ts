export declare class UpdateServerAddress {
    version: number;
    addr: string;
    constructor(version?: any, addr?: any);
    withVersion(version: number): UpdateServerAddress;
    withAddr(addr: string): UpdateServerAddress;
}
