import { NovaLink } from './NovaLink';
export declare class NovaServerImage {
    id: string;
    links: Array<NovaLink>;
    constructor(id?: any, links?: any);
    withId(id: string): NovaServerImage;
    withLinks(links: Array<NovaLink>): NovaServerImage;
}
