import { ServerId } from './ServerId';
export declare class BatchStartServersOption {
    servers: Array<ServerId>;
    constructor(servers?: any);
    withServers(servers: Array<ServerId>): BatchStartServersOption;
}
