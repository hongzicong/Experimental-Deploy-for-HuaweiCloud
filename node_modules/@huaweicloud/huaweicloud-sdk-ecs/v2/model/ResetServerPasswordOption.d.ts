export declare class ResetServerPasswordOption {
    private 'new_password';
    private 'is_check_password'?;
    constructor(newPassword?: any);
    withNewPassword(newPassword: string): ResetServerPasswordOption;
    set newPassword(newPassword: string | undefined);
    get newPassword(): string | undefined;
    withIsCheckPassword(isCheckPassword: boolean): ResetServerPasswordOption;
    set isCheckPassword(isCheckPassword: boolean | undefined);
    get isCheckPassword(): boolean | undefined;
}
